# Gameplay fixes

Requested 2026-09-15. The original five gameplay fixes are implemented and verified. Three additional visual/robot reports were added during implementation; they do not resume unrelated historical backlogs.

## Todo

- [x] **1. Reliable success and next-level navigation.** Reproduce successful shots where the Next level button disappears and Space charges another shot on the same level. Trace authoritative completion, result display, course ordering, and keyboard priority. Every successful campaign result with a following level must visibly offer Next level; clicking it, Space, and N must advance exactly once. Holding Space for fast-forward before completion must not accidentally advance or charge a new shot. Keep retry intentional through R. Give the final campaign level and custom levels an explicit completion/navigation action rather than silently hiding progression. Verify success without a new personal best or top-ten score, and after retries and level changes.

- [x] **2. Safe rapid recalls and retries.** Reproduce throw → repeated R → immediately charge again, including recalls during release and while the previous trajectory is arriving. Trace delayed commands, shot messages, timers, animation and camera state. Repeated recall must be harmless; discarded shots must not move the camera, animate the ball, restore old results, or interrupt the new charge. Test rapid taps, held R, delayed responses and repeated sequences. Preserve immediate retry responsiveness.

- [x] **3. Diagnose degradation over many throws.** Measure before changing cleanup code: compare frame-time percentiles, long frames, retained browser memory, render resource counts, and server/native memory and timing over at least 100 throws after warmup, including rapid recalls and level changes. Inspect shot buffers, replay data, effects, audio, event listeners and timers for accumulation. Distinguish expected bounded caching, garbage collection, rendering cost and network/server stalls. Fix demonstrated causes, then repeat the same workload and report before/after measurements. Completion requires no unbounded per-shot retention and no progressive slowdown in the measured workload, not merely forcing garbage collection or lowering visual quality.

- [x] **4. Correct aim and camera on level entry.** On first entry, Next level, and manual selection, initialize the robot's orientation and the camera from that level's H suggested aim. The reticle and view must be correct before any mouse movement or H press. Clear stale orbit, charge and playback state when changing levels; camera collision handling must still keep the robot visible. Verify after a manually orbited shot, a rapid recall and returning from a replay. Use a sensible target-facing fallback for courses without a hint.

- [x] **5. Improve the opening level sequence.** Audit the opening campaign in gameplay, including actual camera clearance. Start with a simple shot outside the store in an open station area, with clear targets and generous visibility. Progress through basic aiming and charge control, an easy bank, then more constrained interiors and trickier routes. Reorder or replace opening courses as needed; update numbering, first-time entry and progression consistently. Verify every changed course's suggested shot against native physics, including required waypoints and any required destination. Keep only the current course definitions and avoid historical compatibility scaffolding.

## Additional requested fixes

- [x] **6. Find and fix remaining overlapping surfaces.** Audit the current station meshes and procedural details for coplanar/near-coplanar duplicate faces, including wall seams and yellow tactile strips. Reproduce visible flickering while moving and turning, identify the actual conflicting surfaces, and remove duplicates or correct their placement. Verify the affected views in a real browser. Preserve station collision/visual agreement and keep the current station only.

- [x] **7. Fix the robot walk, especially sidesteps.** Reproduce forward, backward and lateral movement with all characters. Correct foot planting, lateral stepping, body balance and transitions into and out of movement without teleporting feet or changing the authoritative walker. Verify normal/fast strafing, direction reversals, stops, slopes and level starts in the real game.

- [x] **8. Bring the robot closer to the social-share artwork.** Inspect the actual current social-share image and compare its silhouette, proportions, face and materials with the in-game robot. Apply the visual direction to the playable model while retaining usable hands, throw animations, celebrations and the existing character choices. Verify the result at gameplay distance and on mobile; do not substitute an unrelated design.

## Verification and completion

- Use one isolated worktree, local service port, database and worker log. No extra agents or machines are required by this queue.
- Reproduce and record the failures first; suspected races and leaks remain hypotheses until measured.
- Test the complete loop in a real browser: enter level → charge → recall rapidly → charge again → finish successfully → Space to next level. Check desktop and mobile result/button layouts.
- Run the relevant regression tests and browser build. Run typecheck and the service suite for service changes, and the isolated runtime suite for physics/protocol/scoring integration. Rebuild the native worker before testing any C# edits.
- Retain authoritative scoring and the rule that a shot ends only when translation and spin stop. Recall forfeits. Do not hide these bugs with timed completion or reduced graphics quality.
- Mark each item complete with reproduction, fix and verification evidence; report any remaining limitations. Deployment requires the user's authorization and must be followed by read-only production checks, without production test scores.

## Goal to send back

Fix and verify all five items in docs/GAMEPLAY-FIX-QUEUE.md, then deploy the completed fixes to production.

## Verification evidence

- **Recall and entry:** delayed trajectory data could revive a recalled shot, and recall after release retained its pending attempt. Attempt fencing now invalidates it immediately; the native reset is acknowledged before a queued new charge starts. Passed 64 native rapid-recall throws at release/early flight, immediate recharging and initial H-aim checks on eight courses. Both macOS and dedicated Linux workers were rebuilt.
- **Progression:** completion now uses the authoritative successful result for the selected course, independently of rank/PB. The action sits above the breakdown and selects the next ordered campaign entry. Real native clears verified Next click, N and Space across the first three stages. Result buttons stayed visible with 44 px minimum height at desktop, 1280×800, 390×844, 320×568 and 844×390. Final/custom and below-top-ten behavior are covered separately by completion tests and local presentation fixtures.
- **Opening sequence:** First Bounce is an open concourse shot with two generous floor waypoints and a downward initial view that shows them. Platform Drift and Terrace Turnaround precede Snack Run and Corner Store. All 30 authored routes passed native waypoint/full-rest proof; the final First Bounce aim was re-proven after the camera adjustment. Two exact and five neighboring throws collected every required waypoint; the destination remains an optional bonus.
- **Measured retention:** 100 throws after ten warmups showed no ordinary per-throw leak before the fix. Another player's replay did leak 64 geometries and ten textures on every enter/exit cycle. Avatar retirement now disposes owned trim, palette, skeleton textures and animation bindings while preserving shared source geometry. With the final robot and station, ten other-player replay cycles remained at 1,084 geometries / 39 textures / 78 programs.
- **Final performance:** same desktop viewport and pixel ratio, real native shots, course changes and repeated recalls: 16.7 ms median, 18.4–18.5 ms p95, 18.6–18.7 ms p99, zero frames over 50 ms across all four 25-throw windows. Before edits: 16.7 / 17.6 / 17.7 ms. Browser heap was 198 MB after warmup and 196 MB after 100 throws; geometry/textures returned to the same counts on revisiting a course. There was no progressive slowdown; the clipped station has a small rendering cost. These are desktop measurements, not a guarantee for every device/network. See `web/benchmarks/GAMEPLAY.md` for reproduction.
- **Surface conflicts:** the exporter now clips only covered portions of overlapping axis-aligned architectural faces within 0.25 mm. It found 106,665 conflicting face pairs and removed 3,108.9 m² of hidden coverage, preserving exposed edges and collision. At Ticket Trick's wall, the paid-gate floor, tactile bases and skyway trim, the original export had two faces at every sampled point. The repaired export has one face and no holes at all 845 samples across those regions plus the upper landing. Swept camera views were checked in the browser. Canonical collision/layout hash is unchanged. Lossless delivery decoding was verified against every exported attribute/material/transform.
- **Robot:** the old lateral gait plunged the pelvis about 19 cm and fast strafing reached 15–17 steps/second. Feet now plant with paced strides, a turning lower body and aim-facing torso; a short lifted pivot handles abrupt reversals. Actual-rig checks cover all characters, 15–120 Hz, both carry styles, forward/back/side/diagonal travel, reversals, slopes and stops; steady pelvis oscillation stays below one micron and cadence below seven steps/second. Ninety walk/turn/stop cases settle within 0.05 mm. Native stairs/escalators and all three characters passed traversal checks.
- **Artwork:** broader cream armor, a larger rounded head, warm curved eyes, dark face/joints, red ear receivers and a front vent bib bring ORI closer to the existing social image. KOMA/DON keep their identities. Both windups, celebrations, sidesteps and gameplay-scale views were reviewed in-browser. 840 actual-rig throw poses retain the native release grip within 0.00000012 m; 60 celebration cases and resource disposal pass.
- **Required checks:** `npm run typecheck`, `npm test`, `npm run test:avatar`, `npm run build:web`, `npm run test:runtime`, full lossless-asset comparison, geometry overlap sampling, campaign proof and native movement checks pass.

## Deployment

- [ ] Merge the verified worktree, publish the current asset pack, deploy the dedicated Linux worker/browser/service, and verify production health and release hashes without creating test scores.
